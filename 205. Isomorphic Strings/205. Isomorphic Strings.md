# 205. Isomorphic Strings

**Difficulty:** Easy  
**LeetCode:** https://leetcode.com/problems/isomorphic-strings/

---

## Problem Statement

Given two strings s and t, determine if they are isomorphic.

Two strings s and t are isomorphic if the characters in s can be replaced to get t.

All occurrences of a character must be replaced with another character while preserving the order of characters. No two characters may map to the same character, but a character may map to itself.

Example 1:
Input: s = "egg", t = "add"
Output: true
Explanation:
The strings s and t can be made identical by:
Mapping 'e' to 'a'.
Mapping 'g' to 'd'.

Example 2:
Input: s = "f11", t = "b23"
Output: false
Explanation:
The strings s and t can not be made identical as '1' needs to be mapped to both '2' and '3'.

Example 3:
Input: s = "paper", t = "title"
Output: true

Constraints:
1 <= s.length <= 5 * 10^4
t.length == s.length
s and t consist of any valid ascii character.

---

## Solution 1: Hash Map with Value Check

```python
class Solution:
    def isIsomorphic(self, s: str, t: str) -> bool:
        mapping = dict()
        for i in range(len(t)):
            if t[i] in mapping and mapping[t[i]] != s[i]:
                return False
            if t[i] not in mapping:
                if s[i] in mapping.values():
                    return False
                mapping[t[i]] = s[i]
        return True
```

### Complexity

| | Complexity | Notes |
|---|---|---|
| **Time** | O(n) | single pass through the strings, but checking values each iteration adds O(n) per step in worst case, making it O(n^2) due to the 'in mapping.values()' call |
| **Space** | O(k) | where k is the number of distinct characters in t (at most n) |

---

